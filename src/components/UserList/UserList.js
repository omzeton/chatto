import React from "react";

import './UserList.css';

const UserList = props => {
  return (
      <div className="UserList">
          <h2>User List</h2>
      </div>
  );
};

export default UserList;
